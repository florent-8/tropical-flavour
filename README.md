# Project Documentation

This project is designed to deploy static files using Cloudflare Workers.

## Setup Instructions

1. Ensure you have Node.js installed.
2. Run `npm install` to install dependencies.
3. Use `npx wrangler deploy` to deploy the project.

## Project Structure

- `code.html`: Main HTML file.
- `DESIGN.md`: Design documentation.
